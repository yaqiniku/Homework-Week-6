CREATE TABLE "object" (  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,  "nama" varchar,  "harga" integer,  "jumlah" integer,  "kategori_id" integer,  "created_at" timestamp DEFAULT 'now()',  "updated_at" timestamp DEFAULT 'now()');CREATE TABLE "category" (  "id" INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,  "nama" varchar,  "created_at" timestamp DEFAULT 'now()',  "updated_at" timestamp DEFAULT 'now()');ALTER TABLE "object" ADD FOREIGN KEY ("kategori_id") REFERENCES "category" ("id");INSERT INTO category(nama)	VALUES 		('Alat Tulis'),		('Sandang');SELECT *	FROM category;UPDATE category set nama = 'Pakaian' WHERE id= 2;INSERT INTO object(nama, harga, jumlah, kategori_id)	VALUES		('Pensil', '1200', '5', '1'),		('Pulpen', '2500', '10', '1'),		('Kaos', '50000', '4', '2'),		('Celana', '35000', '2', '2'),		('Penghapus', '1000', '8', '1');UPDATE object SET harga = '45000' WHERE nama = 'Kaos';SELECT *	FROM object;SELECT 	id,	namaFROM category;SELECT *	FROM category		INNER JOIN object			ON category.id = object.kategori_id;